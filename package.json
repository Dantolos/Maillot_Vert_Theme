{
  "name": "maillot-vert-theme",
  "version": "1.2.0",
  "private": true,
  "description": "Build tooling for the Maillot Vert WordPress theme.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "sass assets/scss:assets/css --style=expanded --no-source-map",
    "watch": "sass --watch assets/scss:assets/css --style=expanded --no-source-map",
    "start": "npm run watch"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
